package Fut.personas;

import Fut.enumLiga.Posicion;

public class JugadorPropio extends Jugador{

    public JugadorPropio(String nombre, int edad, Posicion posicion, int media, double probGol, String equipo) {
        super(nombre, edad, posicion, media, probGol, equipo);
    }
}
